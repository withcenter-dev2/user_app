import 'package:flutter/material.dart';

class AddAvatarScreen extends StatelessWidget{
  const AddAvatarScreen({super.key});

  @override
  build(context)
  {
    return const Scaffold(
      body: Text('Add avatar user'),

    );
  }
}